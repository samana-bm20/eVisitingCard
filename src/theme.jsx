import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#B20000", 
      },
      secondary: {
        main: "#848688", 
      },
      
      tertiary: {
        main: "#335b86", 
      },
    },
  });

export default theme;  